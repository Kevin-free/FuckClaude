---
import { useTranslations, type Lang } from '../i18n/ui';
import { SPONSORS } from '../config/sponsors';

interface Props {
  lang: Lang;
}
const { lang } = Astro.props;
const t = useTranslations(lang);
---

<aside class="sponsors" aria-label={t('sponsors.label')}>
  <div class="wrap sponsors__inner">
    <div class="sponsors__head">
      <span class="sponsors__label">{t('sponsors.label')}</span>
      <a class="sponsors__cta" href="mailto:taokevin1024@gmail.com">{t('sponsors.cta')}</a>
    </div>
    <ul class="sponsors__list">
      {
        SPONSORS.map((sponsor) => (
          <li>
            <a
              class="sponsor"
              href={sponsor.url}
              target="_blank"
              rel="sponsored noopener noreferrer"
            >
              {
                sponsor.logo ? (
                  <img
                    class="sponsor__logo"
                    src={sponsor.logo}
                    width={sponsor.logoWidth}
                    height={sponsor.logoHeight}
                    alt={sponsor.name}
                  />
                ) : (
                  <span class="sponsor__mark" aria-hidden="true">{sponsor.name}</span>
                )
              }
              <p class="sponsor__text">
                <strong>{sponsor.headline[lang]}</strong>
                {sponsor.tagline[lang]}
              </p>
              <span class="sponsor__btn">
                {sponsor.cta[lang]}
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                  <path d="M5 12h14"></path>
                  <path d="m13 6 6 6-6 6"></path>
                </svg>
              </span>
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</aside>
